package com.company;

import java.util.LinkedList;
import java.util.List;

//https://leetcode.com/contest/weekly-contest-343/problems/minimum-cost-of-a-path-with-special-roads/
//6343. Minimum Cost of a Path With Special Roads
public class _6343_ {
    /*public int minimumCost(int[] start, int[] target, int[][] specialRoads) {
        List<int[]> validSpecialRoads = new LinkedList<>();
        //filter out inefficient specialRoads
        for (int[] sr : specialRoads) {
            int actualCost = sr[2]-sr[0]+sr[3]-sr[1];
            if (actualCost>sr[4]) {
                validSpecialRoads.add(sr);
            }
        }


    }

    private int countDist(int x1, int y1, int x2, int y2) {
        return Math.abs(x1-x2)+Math.abs(y1-y2);
    }*/
}
